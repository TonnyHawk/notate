let path = require('path')

export default class serverAPI{
   createFolder(folderData){
      // fetch('http://localhost/puretester.io/server.php', {
      //    method: 'POST',
      //    body: folderData
      // }).then(responce=>responce.text()).then(res=>console.log(res))

   }
}